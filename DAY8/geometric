def is_geometric_progression(seq):
    return all(seq[i] / seq[i-1] == seq[1] / seq[0] for i in range(2, len(seq)) if seq[i-1] != 0)

# Example usage
sequence = [2, 6, 18, 54]
print(is_geometric_progression(sequence))

#output
True
