def is_additive_sequence(seq):
    return all(seq[i] == seq[i-1] + seq[i-2] for i in range(2, len(seq)))

# Example usage
sequence = [1, 2, 3, 5, 8, 13, 21]
print(is_additive_sequence(sequence))

#output
True
